# Base layer — FastAPI process managing Docker container lifecycle.
# Runs on the Jetson host. Serves the Control UI and exposes an API
# for the CLI and web UI to start/stop the Spatial Runtime (ROS layer).
